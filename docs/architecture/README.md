# Architecture Notes

This backend is intentionally built around experimentation and learning, not caption generation.

Core decision engine:
- define hypotheses from trends + account history
- create controlled experiments (format, hook, tone, schedule)
- measure outcomes per objective
- promote winning policies into strategy defaults
